amount_diff_prev = amount_cur - last_amount
amount_ratio_prev = amount_cur / last_amount


trend = mean_last_3 / mean_last_10

Позиция текущей суммы в истории
percentile_rank(amount_cur, amounts)

Регулярность поведения
std(time_deltas between transactions)

Смена timezone
is_timezone_change = (tz_cur != last_tz)

Новизна комбинации device + timezone
is_new_device_tz_pair

Переключение устройства
is_device_switch = (dkey != last_dkey)


is_mcc_switch = (mcc_cur != last_mcc)

session_duration = last_time_in_session - first_time_in_session

session_mean_amount

amount_zscore * is_new_device
amount_zscore * is_new_mcc
is_new_device * is_new_timezone

device_freq

Несколько последних интервалов
delta_1 = t_cur - t_last
delta_2 = t_last - t_prev2
delta_3 = t_prev2 - t_prev3

Относительное ускорение
acceleration = delta_1 / delta_2

Стандартное отклонение интервалов
std_delta_last_k

Коэффициент вариации
cv = std(delta) / mean(delta)

time_since_last_device_change
time_since_last_mcc_change