# Окружение для сборки dataset_cpp (CMake + make + компилятор C++).
# conda env create -f requirements/environment_cpp.yml
# conda activate guard-cpp
name: guard-cpp
channels:
  - conda-forge
  - defaults
dependencies:
  - cmake>=3.16
  - make
  - cxx-compiler
  - openssl
  - arrow-cpp
  - parquet-cpp
